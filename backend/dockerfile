FROM node:24-bookworm-slim

WORKDIR /app
ENV NODE_ENV=production HOST=0.0.0.0 PORT=3000 SERVE_FRONTEND=false

COPY package*.json ./

RUN npm ci --omit=dev

COPY . . 
USER node

EXPOSE 3000

CMD ["sh", "-c", "node database/migrate.js && exec node src/server.js"]
